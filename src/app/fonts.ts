import { Eczar, Open_Sans } from "next/font/google";

export const eczar = Eczar({ subsets: ["latin"], weight: "700" });
export const sans = Open_Sans({
  subsets: ["latin"],
  weight: ["300", "400", "500"],
});
